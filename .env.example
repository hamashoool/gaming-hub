# Gaming Hub Environment Configuration
# Copy this file to .env and update the values before deploying

# ===========================================
# Security Configuration
# ===========================================

# JWT Secret - MUST be changed to a secure random string (minimum 32 characters)
# Generate one with: openssl rand -base64 32
JWT_SECRET=change-this-to-a-secure-random-string-min-32-characters

# JWT Token Expiration (default: 7 days)
JWT_EXPIRES_IN=7d

# ===========================================
# Server Configuration
# ===========================================

# Node Environment
NODE_ENV=production

# Server Port (internal container port is always 3001)
SERVER_PORT=3001

# CORS Origins - Comma-separated list of allowed origins
# For production, set this to your actual domain(s)
# Example: CORS_ORIGIN=https://yourdomain.com,https://www.yourdomain.com
CORS_ORIGIN=http://localhost:3000

# ===========================================
# Client Configuration
# ===========================================

# Client Port (maps to nginx port 80 inside container)
CLIENT_PORT=3000

# API URL - This is baked into the client build
# For production, use your actual API URL
# Example: VITE_API_URL=https://api.yourdomain.com
VITE_API_URL=http://localhost:3001

# ===========================================
# Database Configuration
# ===========================================

# SQLite database path (inside container)
# The database is persisted in a Docker volume
DATABASE_URL=file:/app/data/database.db
